kind: PodMonitoring
metadata:
  name: mlops-api-pod-monitor
  namespace: default
spec:
  selector:
    matchLabels:
      app: mlops-icai
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s